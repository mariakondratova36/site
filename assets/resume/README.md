# Adding your resume

Drop your resume PDF into this folder and name it `resume.pdf`:

```
assets/resume/resume.pdf
```

`resume.html` already points to this exact path, both for the inline preview
and the "Download PDF" button — no other changes needed. Until the file
exists, the page shows a placeholder panel instead of a broken embed.
